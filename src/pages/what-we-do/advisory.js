import React from 'react';
import { PageTemplate, createPageHead } from '../../components/PageTemplate';

const AdvisoryPage = () => {
  return <PageTemplate pageKey="advisory" />;
};

export default AdvisoryPage;
export const Head = createPageHead('advisory');